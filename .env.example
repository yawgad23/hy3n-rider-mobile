# ─── HY3N Rider App — Environment Variables ──────────────────────────────────
# Copy this file to .env and fill in your values before running the app.
#
# The backend (Firebase, Hubtel, email, admin PIN, etc.) lives entirely in
# the hy3n-backend repo's own .env/.env.local — this app only needs to know
# where that backend is running.

EXPO_NO_METRO_WORKSPACE_ROOT=1

# ── Mobile app -> hy3n-backend API ────────────────────────────────────────────
# Point this at the deployed backend (Firebase Cloud Function), or at a
# locally running hy3n-backend instance (iOS Simulator can use localhost; a
# physical device needs your machine's LAN IP, e.g. http://192.168.1.50:3000).
EXPO_PUBLIC_API_BASE_URL=https://api-yvurtipaxq-ew.a.run.app
